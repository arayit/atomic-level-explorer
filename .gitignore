# Journal PDFs are copyrighted and are not redistributable.
papers/

# 300 dpi rasters, regenerable from the data; the vector PDFs are kept.
*.png

.DS_Store
__pycache__/
*.pyc
explorer_fragment.html

# Ladder scan output: hundreds of MB, regenerable by scripts/ladder.py in about an hour.
# The tiered summary it feeds (analysis/materials.*) is small and IS tracked.
analysis/ladders*.csv
analysis/hopstrength.csv
analysis/*.log
